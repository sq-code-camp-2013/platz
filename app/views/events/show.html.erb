<div>
  <h1><%= @event.name %></h1>
  <div>
    <%= @event.street %></br>
    <%= @event.zip_code %>
  </div>
  <div>
    <%= @event.occurs_at.try(:strftime, "%B %d, %l:%M %p") %>
  </div>
  <% if @event.created_by?(current_user) %>
    <%= link_to 'edit', edit_event_url(@event) %>
  <% else %>
    <%= link_to 'attend', edit_event_url(@event) %>
  <% end %>
</div>